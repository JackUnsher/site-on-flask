meta:
  environment: python
  toolchain:
    name: pip
    version: '3.11'

run:
  command: gunicorn -c gunicorn_config.py app:app
  persistenceMount: /data
  excludeFiles:
    - venv
    - .git
    - .pytest_cache
    - __pycache__
    - '*.pyc'
    - htmlcov
    - .coverage 