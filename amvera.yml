meta:
  environment: docker
  toolchain:
    name: docker

build:
  context: .
  dockerfile: Dockerfile
  skip: false

run:
  command: gunicorn -c gunicorn_config.py app:app  
  containerPort: 8080
  persistenceMount: /data 