meta:
  environment: docker
  toolchain:
    name: docker

build:
  context: .
  dockerfile: Dockerfile
  args:
    - "--destination=amvera.registry:5000/app:latest"
  skip: false

run:
  command: gunicorn -c gunicorn_config.py app:app  
  containerPort: 8080
  persistenceMount: /data 