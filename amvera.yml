meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: /workspace/Dockerfile
  args:
    - "--dockerfile=/workspace/Dockerfile"
  skip: false

run:
  persistenceMount: /data
  containerPort: 8080 